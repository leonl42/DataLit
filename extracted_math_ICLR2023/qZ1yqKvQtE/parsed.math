D=\{D_{1},D_{2},\dots,D_{T}\}
t^{th}
D_{t}=\{(\textbf{\em x}_{i}^{t},y_{i}^{t})\}_{i=1}^{N_{t}}
\textbf{\em x}_{i}^{t}\in\mathcal{X}
y_{i}^{t}\in\mathcal{Y}
f_{\theta}:\mathcal{X}\rightarrow\mathcal{Y}
\theta
y=f_{\theta}(\textbf{\em x})\in\mathcal{Y}
t
D_{t-1}
P(\mathcal{X}^{(t)})
P(\mathcal{Y}^{(t)})
t
P(\mathcal{X}^{(t)})\neq P(\mathcal{X}^{(t+1)})
D_{t}
t=\{1,2,\dots,T\}
t
\mathcal{Y}^{(t)}\subset\mathcal{Y}^{(t+1)}
P(\mathcal{Y}^{(t)})\neq P(\mathcal{Y}^{(t+1)})
\mathcal{Y}^{(t)}\neq\mathcal{Y}^{(t+1)}
P(\mathcal{Y}^{(t)})\neq P(\mathcal{Y}^{(t+1)})
t
\mathcal{Y}^{(t)}
\mathcal{Y}^{(t)}=\mathcal{Y}^{(t+1)}
P(\mathcal{Y}^{(t)})=P(\mathcal{Y}^{(t+1)})
P(\mathcal{X}^{(t)})\neq P(\mathcal{X}^{(t+1)})
D_{t}
t
\mathcal{F}=\{\mathbf{E}_{\texttt{visual}},\mathbf{E}_{\texttt{text}}\}
\mathbf{E}_{\texttt{visual}}
\mathbf{E}_{\texttt{text}}
K
\textbf{\em x}_{test}\in\mathbb{R}^{C\times H\times W}
y\in\mathbb{R}^{K}
y_{i}\in\mathcal{Y}=\{y_{1},y_{2},\dots,y_{K}\}
~{}``{\texttt{a photo of a \{category\}}}^{\prime\prime}
\{\textbf{\em p};y_{i}\}
\{\textbf{\em t}_{1},\textbf{\em t}_{2},\dots,\textbf{\em t}_{K}\}
\textbf{\em t}_{i}=\mathbf{E}_{\texttt{text}}(\{\textbf{\em p};y_{i}\})
\textbf{\em x}_{test}
\mathbf{E}_{\texttt{visual}}
\textbf{\em v}=\mathbf{E}_{\texttt{visual}}(\textbf{\em x}_{test})
\textbf{\em s}_{i}={\texttt{sim}}(\textbf{\em t}_{i}\cdot\textbf{\em v})
{\texttt{sim}}(\cdot,\cdot)
\textbf{\em x}_{test}
p(y_{i}|\textbf{\em x}_{test})=\frac{{\texttt{exp}}({\texttt{sim}}(\textbf{\em
t%
}_{i}\cdot\textbf{\em v}))}{\sum_{i=1}^{K}{\texttt{exp}}({\texttt{sim}}(%
\textbf{\em t}_{i}\cdot\textbf{\em v}))}
t
